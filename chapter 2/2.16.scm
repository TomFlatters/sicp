; we have to overcome such problems as 'a(b+c) ≠ ab+ac'
; in other words, we have to implement Interval Arithmetic
; I may come back to this later...

; generally, though, it is because algebraic combination has different rules to interval arithmetic.
; for example, in interval arithmetic, multiplication is not distributive (see top line)